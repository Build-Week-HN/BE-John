const auth = require('./auth.controller');
const comments = require('./comments.controller');
const stories = require('./stories.controller');
const jobs = require('./jobs.controller');
const ask = require('./ask.controller');

module.exports = {
  auth,
  comments,
  stories,
  jobs,
  ask,
};
